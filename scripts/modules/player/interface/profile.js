import { configs } from "../../../core/configs";
import { Player } from "@minecraft/server";
import {
	ActionFormData,
	ModalFormData,
	MessageFormData,
} from "@minecraft/server-ui";

export const Profile = {
	async show(target, watcher) {},
};
