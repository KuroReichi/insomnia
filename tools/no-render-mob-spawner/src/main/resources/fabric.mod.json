{
  "schemaVersion": 1,
  "id": "no_render_mob_spawner",
  "version": "${version}",
  "name": "No Render Mob Spawner",
  "description": "Reduces rendering work from mob spawners while keeping them functional.",
  "authors": ["Kuro", "Rikka"],
  "license": "MIT",
  "environment": "client",
  "entrypoints": {
    "client": ["com.kuroreichi.norenderspawner.NoRenderMobSpawnerClient"],
    "modmenu": ["com.kuroreichi.norenderspawner.NoRenderMobSpawnerModMenu"]
  },
  "mixins": [
    "no-render-mob-spawner.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=0.19.3",
    "minecraft": "26.2",
    "fabric-api": ">=0.160.0"
  }
}
